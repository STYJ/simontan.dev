---
layout: layouts/home.njk
---

{% set maxwritings = collections.writings.length | min(3) %}
<h1>Latest {% if maxwritings == 1 %}writing{% else %}{{ maxwritings }} writings{% endif %}</h1>

{% set writingslist = collections.writings | head(-3) %}
{% set writingslistCounter = collections.writings | length %}
{% include "writingslist.njk" %}

<p>More writings can be found in <a href="{{ '/writings/' | url }}">the archive</a>.</p>
