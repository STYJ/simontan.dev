TODO: Need to filter this by tags then by posts, see <a href="https://www.vrk.dev/categories/">vrk.dev categorized</a>.

{# <ol reversed class="categorisedlist" style="counter-reset: start-from {{ (categorisedlistCounter or categorisedlist.length) + 1 }}">
{% for writing in categorisedlist | reverse %}
  <li class="categorisedlist-item{% if writing.url == url %} categorisedlist-item-active{% endif %}">
    <a href="{{ writing.url | url }}" class="categorisedlist-link">{% if writing.data.title %}{{ writing.data.title }}{% else %}<code>{{ writing.url }}</code>{% endif %}</a>
    <time class="categorisedlist-date" datetime="{{ writing.date | htmlDateString }}">{{ writing.date | readableDate }}</time>
    {% for tag in writing.data.tags %}
      {%- if collections.tagList.indexOf(tag) != -1 -%}
      {% set tagUrl %}/tags/{{ tag }}/{% endset %}
      <a href="{{ tagUrl | url }}" class="writing-tag">{{ tag }}</a>
      {%- endif -%}
    {% endfor %}
  </li>
{% endfor %}
</ol> #}
