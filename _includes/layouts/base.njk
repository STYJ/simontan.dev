<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ title or metadata.title }}</title>
    <link rel="icon" href="img/favicon/favicon.ico" type="image/x-icon">
    <meta
      name="description"
      content="{{ description or metadata.description }}"
    />
    <link rel="stylesheet" href="{{ '/css/index.css' | url }}" />
    <link
      rel="stylesheet"
      href="{{ '/css/prism-base16-monokai.dark.css' | url }}"
    />
    <link
      rel="alternate"
      href="{{ metadata.feed.path | url }}"
      type="application/atom+xml"
      title="{{ metadata.title }}"
    />
    <link
      rel="alternate"
      href="{{ metadata.jsonfeed.path | url }}"
      type="application/json"
      title="{{ metadata.title }}"
    />
  </head>
  <body>
    <header>
      <h1 class="home">
        <a href="{{ '/' | url }}">{{ metadata.title }}</a>
      </h1>
      <sub class="subtitle">
        Musings of a curious developer 📖
      </sub>
      <hr>
    </header>

    <main {% if templateClass %} class="{{ templateClass }}" {% endif %}>
      {{ content | safe }}
    </main>

    <footer>
      <span>
        Inspired by <a href="https://www.vrk.dev/">vrk.dev</a> and <a href="http://bettermotherfuckingwebsite.com/">BMFWS</a>.
        <br>
        Made with ❤️ using <a href="https://github.com/11ty/eleventy-base-blog">11ty base blog</a>.
      </span>
        
    </footer>

    <!-- Current page: {{ page.url | url }} -->
  </body>
</html>
